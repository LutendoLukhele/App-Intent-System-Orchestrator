/**
 * Provider Adapters - IProviderAdapter Implementations
 * 
 * Adapters for external provider integrations (OAuth + API)
 * 
 * Current adapters:
 * - NangoProviderAdapter: Nango-based OAuth/API gateway (Gmail, Salesforce, Slack, etc.)
 * 
 * @package @aso/adapters
 */

export * from './NangoProviderAdapter';
